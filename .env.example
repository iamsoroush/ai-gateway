OPENAI_API_KEY=
GEMINI_API_KEY=
LOG_LEVEL=INFO
# Used when a request omits "model". DEFAULT_AUDIO_MODEL is used when the request
# contains audio; DEFAULT_MODEL is used otherwise.
DEFAULT_MODEL=gpt-5.4-nano
DEFAULT_AUDIO_MODEL=gemini-2.5-flash
